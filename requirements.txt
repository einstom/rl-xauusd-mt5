pandas>=2.0
numpy>=1.24
gymnasium>=0.29
stable-baselines3>=2.3
torch>=2.0
tqdm>=4.66
# Windows-only. Needed on the machine that hosts the MetaTrader 5 terminal
# (data download + live trading). Training itself runs anywhere.
MetaTrader5>=5.0.45; sys_platform == "win32"
